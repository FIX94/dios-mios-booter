DIOS-MIOS Booter is a text-only loader which can boot gamecube games from SD via DIOS-MIOS Lite and also from USB via DIOS-MIOS, also a options menu is available to control all needed game settings, controls are displayed on screen directly.
You can grab pre-compiled builds from the binary folder.